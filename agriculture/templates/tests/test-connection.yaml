apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "agriculture.fullname" . }}-test-connection"
  labels:
    {{- include "agriculture.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "agriculture.fullname" . }}:{{ .Values.django.service.port }}']
  restartPolicy: Never
